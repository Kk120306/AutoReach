import React from 'react'

type TriggerProps = {
    id: string
}

const Trigger = ({ id }: TriggerProps) => {

    const { } = useTrigger();

    return (
        <div>
            
        </div>
    )
}

export default Trigger
